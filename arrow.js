let add = (a, b) => a + b;
console.log(add(23, 23));

let multiply = (a) => a * a;
console.log(multiply(2));

let text = (first, last) => {
  let firtName = first;
  let lastName = last;
  let fullName = firtName + " " + lastName;
  return fullName;
};
console.log(text("xaman", "dawud"));
